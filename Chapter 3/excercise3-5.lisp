;; if we are on a branch print the question and get a yes or no response
;; then choose which child to go to based on that response
;; if we are on a leaf, ask if the leaf is the right answer
;; if it is the right answer then do nothing
;; if it is the wrong answer then ask for the correct answer
;; then ask for a question which will discriminate between the two
;; change the tree to reflect this new information
;; restart the game